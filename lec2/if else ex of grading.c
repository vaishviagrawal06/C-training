#include <stdio.h>
int main(){
    int marks = 73;
    if (marks >= 75) {
        printf("Grade: A\n");
    } else if (marks >= 60) {
        printf("Grade: B\n");
    } else {
        printf("Fail\n");
    }
    return 0;
}
    